## Install anywhere

Optionally set the direnv BIN_DIR (example):

```sh
export DIRENV_BIN_DIR="${HOME}/.local/bin"
```

```sh
curl -fsSL https://raw.githubusercontent.com/andreswebs/features/refs/heads/main/src/direnv/install.sh | sh
```
